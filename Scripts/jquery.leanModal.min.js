// leanModal v1.1 by Ray Stone - http://finelysliced.com.au
// Dual licensed under the MIT and GPL

(function ($) {
    $.fn.extend({ leanModal: function (options) {
        var defaults = { top: 100, closeButton: null }; var overlay = $("<div id='lean_overlay'></div>"); $("body").append(overlay); options = $.extend(defaults, options); return this.each(function () {
            var o = options; $(this).click(function (e) {
                $("#ctl00_userName").focus();
                var modal_id = $(this).attr("href"); $("#lean_overlay").click(function () { close_modal(modal_id) }); $(o.closeButton).click(function () { close_modal(modal_id) }); var modal_height = $(modal_id).outerHeight(); var modal_width = $(modal_id).outerWidth();
                $('#spanInvalis').css("visibility", "hidden");
                document.getElementById("ctl00_userName").value = '';
                document.getElementById("ctl00_passWord").value = '';

                $("#lean_overlay").css({ "display": "block"}); $("#lean_overlay").fadeTo(); $(modal_id).css({ "display": "block", "position": "fixed", "background-color": "#7f0010", "z-index": 11000, "left": 50 + "%", "margin-left": -(modal_width / 2 - 299) + "px", "top": o.top + "px" }); $(modal_id).fadeTo(200, 1); e.preventDefault()
            })
        }); function close_modal(modal_id) { $("#lean_overlay").fadeOut(200); $(modal_id).css({ "display": "none" }) }
    }
    })
})(jQuery);



(function ($) {
    $.fn.extend({ leanModalCenter: function (options) {
        var defaults = { top: 100, closeButton: null }; var overlay = $("<div id='lean_overlay'></div>"); $("body").append(overlay); options = $.extend(defaults, options); return this.each(function () {
            var o = options; $(this).click(function (e) {
                
                var modal_id = $(this).attr("href").replace('kitchenonmyp/','');
                
                var ClickButton = $(this).parent().parent();                 
                $("#lean_overlay").click(function () 
                { 
                //close_modal(modal_id);
                //ClickButton.show();
                }); $(o.closeButton).click(function () { close_modal(modal_id) }); var modal_height = $(modal_id).outerHeight(); var modal_width = $(modal_id).outerWidth();
                $("#lean_overlay").css({ "display": "block"}); $("#lean_overlay").fadeTo(); $(modal_id).css({ "display": "block", "position": "fixed", "background-color": "#7f0010", "z-index": 11000, "left": 50 + "%", "margin-left": -(modal_width / 2) + "px", "top": o.top + "px" }); $(modal_id).fadeTo(200, 1); e.preventDefault()
            })
        }); function close_modal(modal_id) { $("#lean_overlay").fadeOut(200); $(modal_id).css({ "display": "none" }) } 
    } 
    })
})(jQuery);

